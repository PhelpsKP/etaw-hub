# Backend Environment Variables (Cloudflare Workers)

# ===== SECRETS (set via `wrangler secret put <NAME>`) =====
# Never commit these. Set using wrangler CLI.

# JWT signing secret (generate with: openssl rand -base64 32)
# wrangler secret put JWT_SECRET
JWT_SECRET=your-secret-key-here

# MailChannels API key for email service
# wrangler secret put MAILCHANNELS_API_KEY
MAILCHANNELS_API_KEY=your-mailchannels-api-key


# ===== VARS (set in wrangler.jsonc "vars" section) =====

# Allowed CORS origins (comma-separated, production only)
# Local dev always allows http://localhost:5173
ALLOWED_ORIGINS=https://your-app.pages.dev,https://custom-domain.com

# Email sender address
EMAIL_FROM=noreply@yourdomain.com

# Email sender display name
EMAIL_FROM_NAME=Your App Name

# Dry run mode: "true" = log emails without sending, omit/false = send emails
REMINDERS_DRY_RUN=true


# ===== DATABASE =====
# D1 database configured in wrangler.jsonc:
# Binding: DB
# Database: etaw_hub_db
